<!DOCTYPE html>
<html>
    <head>
        <title>Добавление урока</title>

        <meta charset="utf-8">
        <meta name="viewport"
              content="width=device-width, initial-scale=1.0,
              maximum-scale=1.0, minimum-scale=1.0,
              user-scalable=no, minimal-ui">
        <meta name="full-screen" content="yes">

        </head>
    <body>
        <main>

        <form action='/addlesson'  method="POST">
            <div class="lessonAdderForm">
                <h3 class="h3">Название урока</h3>
                <textarea id="title" name = "title" required placeholder="Введите название"></textarea>
                <h3 class="h3">Класс <input id="grade" name = "grade" type="number"/></h3>
                <span style="font-size:11pt;color:#4e4e4e; margin-left:5px; margin-bottom:5px;">(0 - сделает урок доступным для всех классов)</span>
                <h3 class="h3">Описание</h3>
                <textarea id="description" name = "description" placeholder="Введите описание"></textarea>
                <h3 class="h3">Задачи</h3>
                <textarea id="tasks" name = "tasks"  required placeholder="Введите номера задач через пробел"></textarea>
                <button id="submit2">Добавить урок</button>
            </div>
        </form>
    </main>
    </body>
</html>