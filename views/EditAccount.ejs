<!DOCTYPE html>
<html>
    <head>
        <title>Изменение аккаунта</title>

        <meta charset="utf-8">
        <meta name="viewport"
              content="width=device-width, initial-scale=1.0,
              maximum-scale=1.0, minimum-scale=1.0,
              user-scalable=no, minimal-ui">
        <meta name="full-screen" content="yes">

        <link rel="stylesheet" href="/public/css/layout.css" type="text/css">
        </head>
    <body>
        <main style="display: flex; justify-content:center">
            <form class="reg" action= '/EditAccount'  method="POST">
                <% if(user.login.slice(0, 2)=="n_") { %>
                    <h3 class="h3">Ф.И.О.</h3>
                    <input name = "name" value="<%= user.name %>" required>
                    <h3 class="h3">Электронная почта</h3>
                    <input id="email" name="email" type="email" value="<%= user.group %>" required>
                <% } %>
                <h3 class="h3">Пароль</h3>
                <input id="password" name="password" type="password">
                <br>
                <button id="submit2">Изменить</button>
            </form>
            <br>
        </main>
        <h3 style="text-align: center;color:Red"><%= msg %></h3>
    </body>
</html>