<!DOCTYPE html>
<html>
    <head>
        <title>Добавление турнира</title>

        <meta charset="utf-8">
        <meta name="viewport"
              content="width=device-width, initial-scale=1.0,
              maximum-scale=1.0, minimum-scale=1.0,
              user-scalable=no, minimal-ui">
        <meta name="full-screen" content="yes">

        </head>
    <body>
        <main>

        <form action='/tournament/add'  method="POST">
            <div class="lessonAdderForm">
                <h3>Название турнира</h3>
                <textarea id="title" name = "title" required placeholder="Введите название"></textarea>
                <h3>Описание</h3>
                <textarea id="description" name = "description" placeholder="Введите описание"></textarea>
                <h3>Дата начала</h3>
                <input style="width: 90%;margin-left: auto;margin-right: auto;margin-bottom: 10px;" class="dateChooser" type="datetime-local" name="whenStarts" required>
                <h3>Дата окончания</h3>
                <input style="width: 90%;margin-left: auto;margin-right: auto;margin-bottom: 10px;" class="dateChooser" type="datetime-local" name="whenEnds" required>
                <h3>Заморозить таблицу в:</h3>
                <input style="width: 90%;margin-left: auto;margin-right: auto;margin-bottom: 10px;" class="dateChooser" type="datetime-local" name="frozeAfter" required>
                <h3>Разрешить регистрацию после начала турнира</h3>
                <label class="switch" >
                    <input type="checkbox" name="allowRegAfterStart">
                    <span class="slider round"></span>
                </label>
                <button id="submit2">Добавить турнир</button>
            </div>
        </form>
    </main>
    </body>
</html>