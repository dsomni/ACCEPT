<!DOCTYPE html>
<html>
    <head>
        <title>Панель управления</title>

        <meta charset="utf-8">
        <meta name="viewport"
              content="width=device-width, initial-scale=1.0,
              maximum-scale=1.0, minimum-scale=1.0,
              user-scalable=no, minimal-ui">
        <meta name="full-screen" content="yes">
        </head>
    <body>
        <main>
            <h1 class="h1">Настройки пользователя</h1>

            <% if(user){ %>
              <form action='/service/panel/USER'  method="POST">
                <div class="taskAdderForm">
                  <div style="margin-left:5rem">
                    <h2 class="h2">Пользователь <%= user.login %></h2>
                    <input type="text" name="login" style="display:none;" value="<%=user.login%>">
                    <span style="display: inline-block;">
                        <h3 class="h3">Ф.И.О.</h3>
                        <input type="text" name="name" value="<%= user.name %>" style="width: 30ch; margin-top: -1rem;"/>
                    </span>
                    <span>
                        <h3 class="h3">Пароль</h3>
                        <input type="password" name="password" value="<%= user.password %>" style="width: 15ch; margin-top: -1rem;" placeholder="Новый пароль"/>
                    </span>
                    <span>
                      <h3 class="h3">Класс</h3>
                      <input type="text" name="grade" value="<%= user.grade %>" style="width: 4ch; margin-top: -1rem;"/>
                    </span><br>
                    <div style="display: inline-block; margin-top:1rem;  margin-bottom:1rem;">
                      <span>Очистить посылки<input type="checkbox" name="clear"></span>
                      <span style="color:red; margin-left: 2rem">Удалить пользователя<input type="checkbox" name="delete"></span><br>
                    </div>
                  </div>
                    <button>Изменить</button>
                </div>
              </form>
            <% }else{ %>
              <h3>Логин</h3>
              <input type="text" name="login" placeholder="Введите логин" id="search">
              <button class="EditButton" onclick="redir()" >Найти</button>
            <% } %>
        </main>
    </body>
    <script type = text/javascript>
        document.getElementsByTagName('body')[0].removeChild(document.getElementsByTagName('header')[0]);
        document.getElementsByTagName('body')[0].removeChild(document.getElementsByTagName('button')[0]);
    </script>
    <script>
      function redir() {
        location.href = '/service/panel/USER/' + (document.getElementById("search").value || "default")
      }
    </script>
</html>